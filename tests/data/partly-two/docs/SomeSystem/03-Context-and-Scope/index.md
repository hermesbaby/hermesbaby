# Context and Scope

{term}`SomeSystem` is embedded in the following contexts defined in {ref}`sec_catalog_of_contexts`:

- {term}`Some Context`

The SomeSystem interacts with various external actors and software components. The following context diagram
shows the role of these external elements and how they are connected to SomeSystem.

```{figure} _figures/context.drawio.svg
SomeSystem in context {term}`Some Context`
```

```{todo}
Supplement the description of interactions and actors. The text tells "the story" of the system in context.
The context is additionally illustrated in the key points by the diagram.

The following should appear at least once:

- {term}`Some Context`
- {term}`SomeSystem`
- SomePerson
- SomeExternalPerson
- Some External System
```
