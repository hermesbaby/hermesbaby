# Title of b-top-file

A plain sibling file at the root, named so that a numerically-earlier
folder ("1-should-still-be-last-folder") would sort before it if files and
folders were interleaved. They aren't: all direct files come before all
subfolders.
