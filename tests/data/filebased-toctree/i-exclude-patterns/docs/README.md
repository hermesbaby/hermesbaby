# README

Kept only for downward compatibility with reST projects (see conf.py);
excluded from the Sphinx build by conf.py's exclude_patterns, and must
therefore not appear in the generated _toc.yml either.
