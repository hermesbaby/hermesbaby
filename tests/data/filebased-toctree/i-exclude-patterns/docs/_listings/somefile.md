# Title of _listings / somefile

Excluded via conf.py's exclude_patterns. No file here is literally named
"index", so this also exercises the natural-sort fallback-index selection
being pruned away along with the rest of the folder.
